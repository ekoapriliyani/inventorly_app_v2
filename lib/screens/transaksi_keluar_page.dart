import 'package:flutter/material.dart';

class TransaksiKeluarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Transaksi Keluar')),
      body: Center(child: Text('Halaman Transaksi Keluar')),
    );
  }
}
